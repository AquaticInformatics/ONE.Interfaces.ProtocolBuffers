syntax = "proto3";
package claros.instrument;

message LimitData {
    repeated Limit limits = 1;
 
    message Limit {
        uint32 severity = 1;
        uint32 polarity = 2;
        string i18nKey = 3;      
    }
}