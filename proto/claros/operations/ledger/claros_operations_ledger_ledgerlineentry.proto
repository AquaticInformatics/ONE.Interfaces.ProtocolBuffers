syntax = "proto3";
package claros.operations.ledger;
 
import "claros_operations_ledger_ledgerlineentryversion.proto";
 
message LedgerLineEntry {
    uint32 columnId = 1;
    string note = 3;
    uint32 limitStatus = 4;
    repeated LedgerLineEntryVersion versions = 5;
}