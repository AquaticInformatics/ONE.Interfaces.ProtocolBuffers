syntax = "proto3";
package claros.operations.spreadsheet;

option csharp_namespace ="Claros.Operations.Spreadsheet";

enum EnumCellRangeAction {
    ACTION_UNKNOWN = 0;
    ACTION_TAKE_LAST = 1; // Take the value with max rownumber
    ACTION_TAKE_FIRST = 2; // Take the value with min rownumber
    ACTION_TAKE_ALL = 3; // Take all values in range
    ACTION_COMPUTE_TDIF = 4; // Get values needed for TDIF computation
    ACTION_COMPUTE_SINGLE = 5; // Single value available for variable
}