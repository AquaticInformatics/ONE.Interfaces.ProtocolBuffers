syntax = "proto3";
package claros.operations.spreadsheet;
 
enum EnumWorksheet {
    WORKSHEET_UNKNOWN = 0;
    WORKSHEET_FIFTEEN_MINUTE = 1; 
    WORKSHEET_HOUR = 2; 
    WORKSHEET_FOUR_HOUR = 3; 
    WORKSHEET_DAILY = 4;
}