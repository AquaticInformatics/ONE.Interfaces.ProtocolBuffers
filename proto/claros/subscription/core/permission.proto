syntax = "proto3";
package claros.subscription;

import "auditevent.proto";
import "google/protobuf/wrappers.proto";

option csharp_namespace ="Claros.Subscription";
 
message Permission {
	// GUID unique identifier of the permission instance
    string id = 1;

	// nullable api endpoint that the activity can be performed against
	// TODO: Do we need this?? 
	google.protobuf.StringValue resource = 2;

	// nullable CRUD operation
	// TODO: Can this be an Enum? What about Claros.Common.Configuration.Rights?
    google.protobuf.StringValue activity = 3;

	// Audit information for the permission
    claros.common.core.AuditEvent auditEvent = 99;
}
