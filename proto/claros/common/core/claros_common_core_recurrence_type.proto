syntax = "proto3";
package claros.common.core;
 
enum RecurrenceType {
    RECURRENCE_TYPE_UNKNOWN = 0;
    RECURRENCE_TYPE_FIFTEENMINUTE = 1;
    RECURRENCE_TYPERHOURLY = 2;
    RECURRENCE_TYPE_FOURHOUR = 3;
    RECURRENCE_TYPE_DAILY = 4;
    RECURRENCE_TYPE_WEEKLY = 5;
    RECURRENCE_TYPE_DAYSOFWEEK = 6;
}
 