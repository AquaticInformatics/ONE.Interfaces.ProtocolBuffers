syntax = "proto3";
package claros.common.computation;

enum ComputationCursorType
{
    COMPUTATIONCURSORTYPE_UNKNOWN = 0;
    CURRENT = 1;
    PREVIOUS = 2;
    NEXT = 4;
    LAST = 5;
    ABSOLUTE = 6;    
}