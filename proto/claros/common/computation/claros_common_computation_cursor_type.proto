syntax = "proto3";
package claros.common.computation;

enum ComputationCursorType
{
    COMPUTATION_CURSOR_TYPE_UNKNOWN = 0;
    COMPUTATION_CURSOR_TYPE_CURRENT = 1;
    COMPUTATION_CURSOR_TYPE_PREVIOUS = 2;
    COMPUTATION_CURSOR_TYPE_NEXT = 4;
    COMPUTATION_CURSOR_TYPE_LAST = 5;
    COMPUTATION_CURSOR_TYPE_ABSOLUTE = 6;    
}