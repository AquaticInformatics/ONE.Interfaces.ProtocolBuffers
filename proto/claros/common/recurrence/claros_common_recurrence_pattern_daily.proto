syntax = "proto3";
package claros.common.recurrence;

message Daily {
    oneof dailyPattern {
        // Every x number of days
        uint32 interval = 1;
        // Recurrance every Monday through Friday
        bool everyWeekday = 2;
    }
}