syntax = "proto3";
package claros.common.recurrence;

option csharp_namespace ="Claros.Common.Recurrence";

message PatternDaily {
    oneof patternDaily {
        // how many days between each occurance
        uint32 dailyInterval = 1;
        // occurs every Monday through Friday
        bool everyWeekday = 2;
    }
}