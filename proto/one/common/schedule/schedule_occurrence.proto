syntax = "proto3";

option csharp_namespace ="ONE.Models.CSharp";

message ScheduleOccurrence {

    // the list of occurrences 
    repeated string occurrences = 1;
}

message ScheduleOccurrences {
    repeated ScheduleOccurrence items = 1;
}
